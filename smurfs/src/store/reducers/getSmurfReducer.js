export let initialState = {
    isFetching: false,
    error: '',
    
}

export let getSmurfReducer = (state = initialState, action) => {
    switch ( action.type) {

        default:
            return state
    }
}