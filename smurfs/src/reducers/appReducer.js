const initialState = {
        name: "Brainey",
        age: 200,
        height: "5cm",
        id: 0
    }


function appReducer(state, action) {
    switch(action.type) {
        default state;
    }
}