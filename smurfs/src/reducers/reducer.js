import {
  START_FETCHING,
  FETCH_SUCCESS,
  FETCH_FAILURE
} from "../actions/actions";

const initialState = {
  smurfs: [
    {
      name: "Brainey",
      age: 200,
      height: "5cm",
      id: 0
    }
  ]
};

const reducer = (state = initialState, action) => {
    switch(action.type) {
        case: 
        const newSmurf = {
            name: 
        }
    }
};

export default reducer;
